<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GameEndActivity"></activity>
        <activity android:name=".GameWaitActivity" />
        <activity android:name=".GameStartActivity" />
        <activity android:name=".TutorialEndActivity" />
        <activity android:name=".TutorialStartActivity" />
        <activity android:name=".ReadUserInfo" />
        <activity android:name=".RankingActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".MyInfomationActivity" />
        <activity android:name=".AdviceActivity" />
        <activity android:name=".FindInfomationActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".lobyActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>